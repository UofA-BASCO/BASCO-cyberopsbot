{
  "name": "cyberopsbot",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "fix": "eslint --cache --cache-location .cache/.eslintcache --fix *.js src/**",
    "lint": "eslint --cache --cache-location .cache/.eslintcache *.js src/**",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install --save; node app.js"
  },
  "author": "DJ Kempson (DJDaDingo)",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.3.1"
  },
  "devDependencies": {
    "standard": "^14.3.4"
  }
}
